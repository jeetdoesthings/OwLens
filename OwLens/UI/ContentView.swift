import SwiftUI

/// Legacy — root view is OwLensApp.RootView. This file exists for
/// Xcode project compatibility and has no independent function.
struct ContentView: View {
    var body: some View {
        RootView()
    }
}

#Preview {
    ContentView()
}
