import SwiftUI

/// Kept for previews / legacy references — production UI is `RootView` in OwLensApp.
struct ContentView: View {
    var body: some View {
        RootView()
    }
}

#Preview {
    ContentView()
}
